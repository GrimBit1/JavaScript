console.log("Hi");


// -- Local Storage

// ! Local storage is just like cookie but it survives the browser restart

//* Syntax

// ? localStorage.setItem(`${key}`, `${value}`) //  to set a new key value in the local storage

// let a = prompt("Enter the key u want to set?");

// let b = prompt("Enter the value u want to set?");


// * Local Storage Methods

// ? localStorage.setItem(a, b); // to set a item

// ? localStorage.getItem(a); // to query or get a value respective to key

// ? localStorage.removeItem(a) // to remove item with respective to key

//! localStorage.clear() // to clear all the key value pairs

localStorage.key(0) // to access the value with respect to index

// All the data are stored as JSON

localStorage.length // to get the length