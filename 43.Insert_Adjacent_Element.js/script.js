console.log("Hi")

let first = document.getElementById("first")

// first.insertAdjacentHTML("beforeend",'<div >Hi im Adjacent</div>')// It insert the html just before the end of first

// first.insertAdjacentHTML("afterend",'<div >Hi im Adjacent</div>') // It insert the html just after the end of first

// first.insertAdjacentHTML("afterbegin",'<div >Hi im Adjacent</div>')// It insert the html just after the start of first

// first.insertAdjacentHTML("beforebegin",'<div >Hi im Adjacent</div>')// It insert the html just before the start of first

first.remove() // Removes the node